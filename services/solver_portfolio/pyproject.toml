[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "solver_portfolio"
version = "0.1.0"
description = "RNAOS Solver Portfolio"
requires-python = ">=3.11"

dependencies = [
    "optimization",
    "ai_intelligence",
    "rna_intelligence",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
