[pytest]

pythonpath =
    services/deep_learning/src
    services/validation/src

python_files =
    test_*.py

python_classes =
    Test*

python_functions =
    test_*

addopts =
    -ra
    --strict-markers
    --disable-warnings
    --import-mode=importlib

markers =
    unit
    integration
    benchmark
